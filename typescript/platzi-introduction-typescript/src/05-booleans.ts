(()=>{
let isEnable = false;
isEnable = true;

const random = Math.random();
console.log(random);
let isNew = true;
// When we make an evaluation, the program identifies we need to provide only a boolean type of output.
isNew =  random >= 0.5 ? true : false;
})();
