import { Product } from "./product.model";
import { Size } from "./product.model";

const createProduct = (data: Product) => {
  console.log(`Product: ${data.name}; cost: ${data.cost}; desc: ${data.description}`)
};

const rta = 1 + "1";
